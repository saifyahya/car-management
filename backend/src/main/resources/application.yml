server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

  h2:
    console:
      enabled: false

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${GMAIL_USERNAME:saifbarakat991@gmail.com}
    password: ${GMAIL_APP_PASSWORD:qtruhgoarbzgtihj}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  freemarker:
    template-loader-path: classpath:/templates/email/
    suffix: .ftlh
    charset: UTF-8
    cache: true

app:
  public-base-url: ${PUBLIC_BASE_URL:http://localhost:4200}

  sms:
    provider: ${SMS_PROVIDER:mail}

  mail:
    from: ${GMAIL_USERNAME:saifbarakat991@gmail.com}

  twilio:
    account-sid: ${TWILIO_ACCOUNT_SID:}
    auth-token: ${TWILIO_AUTH_TOKEN:}
    from-number: ${TWILIO_FROM_NUMBER:}

  jwt:
    secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}