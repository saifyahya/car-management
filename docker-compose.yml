services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: valet
      POSTGRES_USER: valet
      POSTGRES_PASSWORD: valet
    ports:
      - "5432:5432"
    volumes:
      - valet-postgres:/var/lib/postgresql/data
volumes:
  valet-postgres:
